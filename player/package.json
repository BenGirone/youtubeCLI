{
  "name": "youtubecli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build-win": "electron-packager . youtubecli --platform=win32 --arch=x64 --overwrite",
    "build-lin": "electron-packager . youtubecli --platform=linux --arch=x64 --overwrite"
  },
  "author": "Ben Girone",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.5.0"
  },
  "devDependencies": {
    "electron": "^7.1.3",
    "electron-packager": "^14.1.1"
  }
}
